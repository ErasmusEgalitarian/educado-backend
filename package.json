{
  "dependencies": {
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "cross-env": "^10.1.0",
    "develop": "^1.0.0",
    "lucide-react": "^0.544.0",
    "strapi": "^3.6.11",
    "tailwind-merge": "^3.3.1",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix web\" \"npm run dev --prefix strapi\"",
    "compose": "docker compose up -d strapiDB",
    "install:root": "npm install",
    "install:web": "npm install --prefix web",
    "install:strapi": "npm install --prefix strapi",
    "install-all": "npm-run-all install:root install:web install:strapi"
  }
}
